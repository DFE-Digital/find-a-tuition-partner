@page
@model UI.Pages.CompareListClearConfirm
@{
    var returnUrl = "/compare-list?" + Model.Data.ToQueryString();
    ViewData["Title"] = "Clear my price comparison list confirmation";
    ViewData["BackLinkHref"] = returnUrl;
}

<div class="govuk-grid-row">
    <div class="govuk-grid-column-full">
        <form method="post">
            <h1 class="govuk-heading-l">You're about to clear your price comparison list</h1>

            <p class="govuk-body">
                Any tuition partners you’ve added to your price comparison list will be removed.
            </p>

            <div class="govuk-button-group">
              <button class="govuk-button" data-testid="call-to-action" data-module="govuk-button" type="submit" asp-page-handler="remove" asp-all-route-data="@Model.Data.ToRouteData()">
                Clear my price comparison list
              </button>
              <a class="govuk-link" data-testid="cancel-link" href="@returnUrl">Cancel</a>
            </div>
        </form>
    </div>
</div>